# 验收记录 · Phase 0（安全网）· 2026-08-20

执行：subagent-driven（每任务 implementer + spec 评审）；控制器复核。**未 commit**（Iron Law，RED-7 ✓）。

| AC/RED | 结果 | 证据 | 备注 |
|---|---|---|---|
| AC-A0（目标清单冻结） | PASS | objectives v1.1 FROZEN 头 + 54 项引用零悬空（spec-rev-0-0 全查） | 无新增编号；parameterMap 死字段并入 G-DEAD-3 证据 |
| RED-1（0 未鉴权端点） | PASS（代码+产物级） | T0.1 diff 6 注解恢复；mvnd 50 绿；javap 部署 jar：5×hasPermi + RuntimeVisibleAnnotations + 6 方法（/tmp/qcmv/dump.txt） | **403 实测遗留 Phase 1.8**（见下） |
| RED-2（0 `${}`） | PASS | T0.2：两处改 #{}；MapperXmlInjectionGuardTest 反向注入验证红→绿；全目录 `${` 归零 | 守卫为目录级扫描，覆盖 Phase 1 新 XML |
| AC-D1（鉴权覆盖+三态） | PASS | ① 无 token → body code **401**（`{"msg":"...认证失败","code":401}`）；② 无权限账号 → **403**（`{"msg":"没有权限，请联系管理员授权","code":403}`，report/list 与 report add 双端点）；③ Admin token → **200**（total:6）；产物级：javap 部署 jar 5×hasPermi + RuntimeVisibleAnnotations | 403 账号 = adm_test_403（userId 99999，零角色零权限，经管理员 API resetPwd 至 Qcm@403Test——dev 库测试账号，后续门禁复用） |
| G-SEC-1 CLOSED | PASS | T0.1（6 端点：list/export=list、query/add/edit/remove） | mvnd test 50/0/0 |
| G-SEC-2 CLOSED | PASS | T0.2 | 含守卫测试（新增 1 测试，总数 49→50） |
| G-PERF-2 CLOSED | PASS | T0.3 单行删除；Report/Records 的 startPage 在 MyBatis 查询前属合法保留 | getDataTable 包装内存列表 total=size 行为不变 |
| G-BUG-1 CLOSED | PASS | T0.4 ConcurrentHashMap + HashMap import 清除 | 同类排查：仅 parameterMap 死字段（→G-DEAD-3） |
| mvnd test 全绿 | PASS | `Tests run: 50, Failures: 0, Errors: 0, Skipped: 0, BUILD SUCCESS` | 含新增守卫测试 |
| 核心运行时 | PASS | mvnd install → restart-core.sh（PID 388080，cwd=workspace 根 ✓）→ 8080 up | 加载修复后 jar（jar 15:24 / core 重启 15:24） |

## 目标清单状态（Phase 0 行）

CLOSED 4：G-SEC-1、G-SEC-2、G-PERF-2、G-BUG-1（证据见各条状态列）；WAIVED 0；新增 0。

## 遗留与移交

1. ~~403 边界实测~~ **已闭合**（用户指引登录页写死的管理员账号 → API 自助 resetPwd adm_test_403 → 双端点 403 实测过）。Phase 1.8 鉴权回归复用账号 adm_test_403 / Qcm@403Test。
2. Phase 0 期间未发现清单外新异味/漏洞（新增条目 0）。
3. 工作区状态：4 文件修改（ReportController / RecordsMapper.xml / GasSettingController / Integration 入口）+ 2 新增（MapperXmlInjectionGuardTest / docs），全部未 commit，待人工检查。

执行人：subagent impl-0-0..0-4 + spec-rev-0-0/0-1/0-2；评审人：控制器（Claude）；用户确认：____
