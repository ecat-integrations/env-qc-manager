# 验收记录 · Phase 2（调度器与计划页）· 2026-08-21

执行：subagent-driven（T2.1/2.2/2.3/2.4/2.5 implementer+spec 评审，T2.3 另经 D-1/D-2 打回修复、T2.1 经不可变性缺陷修复、T2.2 经双触发竞态打回修复）；T2.6/T2.7 控制器执行。**未 commit**（RED-7 ✓）。

| AC/RED | 结果 | 证据 |
|---|---|---|
| AC-A2（调度语义测试） | PASS | ScheduleCalculatorTest 17 用例（四类调度+缺日跳月+窗口闭区间+不追补+不可变性锁定）+ ScheduleSpecsTest 6 + QcmPlanSchedulerTest 12（含双触发竞态/写失败热循环防护） |
| AC-A4（ONCE 终态） | PASS | 单测（成功/失败/冲突三态收口）+ misfire 统一终态决策（评审裁决记录） |
| AC-A5（批次/终止） | PASS | 单测批次 N 行/批次终止；**浏览器实测**：受理批次 203a1ffa… → 冲突批次 6f71e0f5… 双批落库；中止 → STOPPING → 1 分钟收敛 FAILED「流程被用户手动终止」+end_time（G-BUG-2 实证） |
| AC-A6（触发溯源） | PASS | 记录页可见：trigger_user=Admin7s9k2G5（真实登录用户）/task_type=1（手动）/failure_reason=EXECUTOR_BUSY_CONFLICT/record_snapshot ✓ |
| AC-A13（计划页全链路） | PASS | 浏览器实测：seed 18 条渲染（中文类型标签/多仪器显示/摘要文案）；五步向导（每周双日 02:00+跨度 SO2+400ppb+流量默认+时长预填）；创建成功→ACTIVE→next_fire=2026-08-24 02:00（周一，纯函数正确）；立即执行受理+冲突拒绝；删除成功 |
| AC-B2/B3（确定性/覆盖） | PASS | 全部测试零 sleep（Clock 注入/CapturingExecutor）；模块 191/191 绿（T2.1 +17、T2.2 +15、T2.3 +15、T2.4 +73、守卫 +1） |
| AC-C4（唤醒精度） | 设计 PASS（ms 级 schedule）+ 调度器启动日志（armedPlanId 机制） | 到点触发语义由虚拟时钟单测锁死 |
| AC-F1/F4/F5（前端基线） | PASS | 100% element-plus（el-steps/el-time-picker/el-date-picker/el-checkbox-group）；dist 重建（1133916→1175475B）；回退保留/状态显隐实测 |
| FR-01-32（预估同源） | PASS | 浏览器实测第五步预估区：跨度检查 23分20秒/自动校准 3分8秒/恢复 2分钟/**总 29分01秒**（composer estimatePhases 只读同源，跨模块 +21 行已声明） |
| RED-2/3 维持 | PASS | 守卫测试扩展（insert 列/值对称性）绿；新代码零吞异常 |
| RED-9 | PASS | package.json 零改动、无 tw: |

## 事件与修复（本 Phase 抓到 4 个运行时缺陷，全部当轮修复）

1. **G-BUG-12**（insert 列/值不对称 13:12）→ 修复 + **静态对称守卫**（防复发，mock 盲区教训）。
2. **G-BUG-13**（Spring bean 签名/lambda 合成方法含 composer 类型 → ruoyi 类加载器内省 CNFE → bean 失败整链 404；附带 PlanParamValidator @Component 陷阱复发）→ 签名 Object 化 + javap 全 bean 类扫描归零 + @Component→@Service。**新经验**：动态 jar 集成的 Spring bean 方法签名/lambda 参数/返回类型不得引用其他集成的类（旧代码全在方法体内所以从未触发）。
3. **G-BUG-14**（insertBatch update_time NOT NULL 未赋值）→ buildRecords 显式补两时刻。
4. T2.2 评审打回：双触发竞态（孤儿任务）+ 写失败热循环 → 归属清理+指纹闩双闸修复（红绿验证）。

## 目标清单状态（Phase 2 行）

CLOSED 新增 12 项（累计 40/58）；WAIVED 0；新增 3（G-BUG-12/13/14，均已 CLOSED）。

## 遗留与移交

1. G-BUG-8（Task 惰性 mry 字段）→ Phase 5 随规范收口（薄化后风险更低）。
2. multi_zero_check 前端类型标签缺失（显示原始 code）→ Phase 4 随新类型补。
3. estimate 的 composer 只读方法（+21 行）已向用户声明为跨模块最小增量。
4. 调度到点自动触发（SCHEDULED 源全链路）：语义由单测+虚拟时钟锁死；运行时到点触发未安排 24h 等待实测（next_fire 最近 seed 计划为 PAUSED；建议终验前启用一条次日计划做一次真实到点观察——已在终验清单登记）。

执行人：subagent impl-2-1..2-5 + spec-rev 系列；控制器执行 T2.6（OPEN-09 核对：旧代码从未消费 calculatedValue，seed NULL=精确等价）与 T2.7（浏览器全链路）；用户确认：____（自主模式，事后审）
