# 06 · 种子与迁移

> 决策依据：D2（旧 job 停用、出等价 seed）、D13（表换代，DDL 归 qcm 模块）。
> 前提：无生产数据，直接改种子，不写迁移代码。

## 1. 旧 sys_job 处置

- **FR-06-01** 从 ruoyi 种子（`public.sql` / `public_no_shard.sql`）删除 sys_job id 21-41（任务组「质控任务」全部 21 条，`public.sql:2404-2429`）（⚠️ OPEN-02：默认删除；备选仅置暂停态保留）。
- **FR-06-02** 字典 `sys_job_group` 的「质控任务/QualityControl」项（`public.sql:2199`）**保留**：历史 sys_job_log 关联可读，且前端 jobGroup 过滤不因缺项报错。
- **FR-06-03** 已部署库的人工处置 SQL 以注释形式附在 `qcm_data.sql` 文件头（DELETE sys_job 21-41），随种子重灌流程执行。
- **FR-06-04** 相邻 job 不动：id 15（质控报告定时生成，DEFAULT 组）、**42/43/44/45**（人工核查参数任务 ManualCheck 家族全四条：NO2/CO/O3/SO2——初版误记 42/43 两条，T1.7 实施时发现 44/45 属同家族曾误删、已恢复）均不在「质控任务」组，维持现状。

## 2. 旧数据表处置（D13）

- **FR-06-05** `env_quality_control_plan` / `env_quality_control_records` / `env_quality_control_report` 三表 DDL 从 ruoyi `public.sql`、`public_no_shard.sql`、`public_migrate.sql` 种子中全部删除。
- **FR-06-06** `qcm_data.sql` 文件头附已部署库人工 DROP 三表 SQL 注释；新库部署顺序变为：ruoyi `public.sql`（不再含 qcm 表）→ `qcm_data.sql` → `qcm_auth.sql`。

## 3. 等价 plan seed

- **FR-06-07** `qcm_data.sql` 内附 18 条计划 seed，初始状态全部 **PAUSED**（⚠️ OPEN-08，同现状 21 条 job 全暂停；部署后由用户人工启用）。等价性映射：原 21 条 job 中 4 条零点合并为 1 条多仪器零点计划（新类型，D4「新需求用新的」），其余 17 条一一对应：

| # | 质控类型 | 仪器 | 调度 | 浓度 | 流量 |
|---|---|---|---|---|---|
| 1 | 多仪器零点质控（新） | [SO2, NO2, CO, O3] | 每日 00:00 | — | — |
| 2-5 | 跨度核查 | SO2 / NO2 / CO / O3 各一 | 每日 00:00 | SO2/NO2/O3=400ppb；CO=40000ppb（原 job 0.4/40 ppm 换算） | 默认 4.0 |
| 6-9 | 线性核查 | 同上四气体 | 每日 00:00 | 百分比序列默认 [0,0.1,0.2,0.4,0.6,0.8] | 默认 4.0 |
| 10-13 | 精密度核查 | 同上四气体 | 每日 00:00 | — | 默认 4.0 |
| 14-17 | 准确度核查 | 同上四气体 | 每日 00:00 | 百分比序列默认 [0.1,0.2,0.4,0.6,0.8] | 默认 4.0 |
| 18 | 转换效率检查 | NO2（NOx） | 每日 00:00 | — | 默认 4.0 |

- **FR-06-08** ⚠️ OPEN-09：原 job 第三参 `calculatedValue` 在 multi / precision / accuracy / conversion 类型传 `'1'`，其语义（跨度比例？量程倍数？）需与 `EnvQualityControlTask` 参数解析核对后落实 seed 映射；核对前上述行只给默认参数（duration_overrides 为空=全默认）。
- **FR-06-09** 人工核查（audit_span）现状无定时条目（组 ManualCheck 的 42/43 是手动参数任务），不出计划 seed；页面可创建（D6 已纳入可计划）。
- **FR-06-10** seed 中计划名沿用原 job 语义命名（如「零点检查-SO2」→ 合并计划名「零点质控-全部仪器」；「跨度核查-SO2」同名保留），created_by=「system」。

## 4. 新类型接线前的 seed 行为

- **FR-06-11** seed 中多仪器零点计划（#1）保持 PAUSED；若部署后被启用并触发 → 产生 `EXECUTOR_TYPE_NOT_READY` 失败记录（02 §FR-02-14），不阻塞其他 17 条计划。
- **FR-06-12** composer 新 flow 接线完成后该计划无需改 seed 即可正常运行（类型契约不变）。

## 5. 部署与验证清单

- **FR-06-13** 新库全量重灌：`public.sql` → `qcm_data.sql` → `qcm_auth.sql`；验证：
  1. `#/taskjob/job` 页任务组「质控任务」不再出现 21-41；
  2. 8081 出现「质控任务计划」菜单与页面，18 条 seed 计划可见（全暂停态）；
  3. 对一条非新类型 seed 计划（如跨度核查-SO2）执行「立即执行」→ 产生执行记录（含 trigger_user=登录用户、batch_id、plan_id）；调度到点触发语义由虚拟时钟单测覆盖；
  4. 执行中再次触发（手动立即执行）→ 被拒 + 失败记录 EXECUTOR_BUSY_CONFLICT + 页面提示；
  5. 记录页 / 报告页在新表上功能正常（重绑回归）。
- **FR-06-14** 已部署库升级路径：执行 FR-06-03 / FR-06-06 的人工 SQL → apply `qcm_data.sql`（建表 + seed）→ apply `qcm_auth.sql` → 重启 core。
