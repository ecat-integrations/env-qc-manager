# 验收记录 · 变更批（用户 review 四项反馈）· 2026-08-21

流程：实事求是分析（根因证据给用户 review）→ 用户确认（认可/做/可以）→ TDD 开发（每项红→绿）→ 浏览器逐项实证。**未 commit**。

| 反馈 | 根因 | 修复 | 浏览器实证 |
|---|---|---|---|
| ① 每月勾选不及时 | element-plus `checked` prop 非响应式受控 + group 无 v-model（手搓三件套 UI 断裂，数据其实已入 model） | checkbox 组改与每周同构（v-model + :value，删 toggleMonthDay） | 1/15/28 号勾选渲染 ✓、未点 2 号 false ✓ |
| ② 浓度展示 | 设计缺口（原「无需浓度」信息量不足；量程/单位知识前端缺失） | D19：零点只读 0+禁用框；多点/准确度浓度点表（composer 只读 estimatePoints +68 行，单位后端统一 CO=ppm）；跨度输入单位随仪器（CO=ppm 自动换算） | 零点显示 0/固定文案/禁用框 ✓；多点 CO 点表 0%/10%/…/80% → 0/5/10/20/30/40 **ppm** ✓；CO ppm 单位+换算提示 ✓ |
| ③ 流量不能设 | 我按原始需求「零点不需要流量」实现（设计依据） | D19 需求变更：全类型可设默认 4.0（Validator 删类型拒绝；后端 buildFlowParams 本就全类型透传） | 零点类型流量步出现、默认 4.0 可改 ✓ |
| ④ 采样 20 预估不变 | **buildFlowParams 的 zero/span 分支丢 durationOverrides**（平移旧代码保留的旧限制，违反 FR-01-34；estimate 与真实执行同断） | zero/span 分支补 putAll（TDD 红：expected 20 but was null → 绿）；第五步「恢复默认值」按钮 | 采样 3→20：总预估 29分01秒→**37分31秒**（+8分30秒=17×30s 精确命中公式）✓；重置回 29分1秒/采样回 3 ✓ |

## 过程事件
1. **部署陷阱复现**：前端 agent 重建 dist 后未重打 jar，浏览器一度仍旧版——`mvnd install` + 重启后恢复（jar 内资源以打包为准，memory 既有教训的再次实证）。
2. **回归发现并修复**：浓度点表 watcher 只在「步骤内变更」触发，从步骤 2 带入选好的类型/仪器进步骤 3 不触发 → 补 step 进入即拉取的 watcher。
3. **E2E 到点触发（用户要求项）✅**：seed「跨度核查-SO2」改 06:48 到点并启用 → 06:48:00 整触发，SCHEDULED 记录（task_type=0/trigger_user=system/快照✓）落库，next_fire 自动推进次日 06:48。该次执行被我部署重启打断，断电恢复机制将其正确收敛为「任务运行期间意外中止」（G-BUG-3 修复路径的真实场景意外实证）。
4. composer 增量 2 处累计：estimatePhases（+21 行，Phase 2）+ estimatePoints/applyParams（+68 行，本批）——均只读、不动执行链。

## 验证
- qcm **235/235** 绿（226→235，+9 TDD 新用例）；composer **144/144** 绿（138→144）。
- 测试链路：buildFlowParams zero/span 透传红→绿、Validator 流量规则红→绿（3 用例翻转）、estimatePoints 纯函数（默认/CO 量程/覆盖三态）、estimate points 映射与 CO 单位换算。
- 需求文档已同步：D19 决策 + FR-01-27/30/26 修订 + 新增 FR-01-44/45/46。
- 目标清单：61/61 全 CLOSED（变更批 3 项：G-BUG-15/16、G-REQ-9）。

执行人：subagent impl-fix-batch-be/fe + 控制器（watcher 补丁/回归/对账）；用户确认：2026-08-21「1 认可 2 做 3 可以」

## 附：完整执行 E2E（用户质询「都没有成功」后补做）✅

- 前三条记录归因：id=3 手动终止（功能测试故意）、id=4 BUSY 冲突（D10 测试故意）、id=5 部署重启打断（我的时机失误）——均非执行链路缺陷，但缺完整跑通证据。
- **完整 E2E**：07:22:04 经 REST 立即执行（批次 cf7ce962，记录 id=6，跨度核查 SO2 400ppb）→ 全程未重启 core → **07:45:58 到终态 SUCCESS（2）**，实际 23 分 54 秒。
- 阶段实际耗时 vs 预估：prepare 14.2s/15s · setup 8.9s/18s · **check 1410.7s/1400s（精确吻合）**；「自动校准（按需）」因判定未通过按需跳过；总时长与三阶段之和吻合。
- 质控结论分层：执行状态=成功，质控结论=不合格（sim 读数 vs 400ppb 标气判定，属质控结果语义非执行失败）——记录页两列分层正确展示（成功 | 质控结果-不合格）。
- 过程小坑（工具侧）：终态监控脚本 tuple 提取 bug 导致超时误报（执行实际 07:45 已完成），已确认非产品问题。


## 附 2：不合格任务报告链路（用户 review「不合格也要有报告，结论不通过」）✅

- 链路核查：报告生成按执行状态=SUCCESS 选记录（未通过不挡，完成即 SUCCESS 语义 ✓）、ZeroSpanDayPairSelector 单边 span 出报告 ✓——**设计本就支持**；
- 抓到两处真断点并修（G-BUG-17/18）：① saveBatch 漏盖 update_time → 报告生成 1 条存储 0 条（线上日志实证）；② 预览/导出闸 Objects.equals(Integer,Long) 恒 false → 成功执行也被拒（T1.4 评审预告的根因首次真实咬人）；
- 修复后全链路实证（job 15 手动重触发）：报告落库（span_calibration_result=**不合格**、结果值 -96.125/标准 400.0/设备值 15.5）→ 报告页列表渲染 → 记录页「质控结果」弹窗完整（不合格徽章+报告全文+**阶段时间线**）+ 导出 PDF 入口；
- qcm 236/236 绿（+1 G-BUG-17 回归锁）；测试效率：采纳用户建议——后续执行类 E2E 用 durationOverrides 缩短时长（如 stableTime=60/sampleCount=1/sampleInterval=5 → check 阶段约 2 分钟），本次报告链路验证复用已有记录零等待。


## 附 3：报告页演示占位数据（用户 review）+ 五页面默认行为系统检查 ✅

- 根因：report/index.vue 列表初始值=本地 mockTableData（3.7KB 演示数据），首载先渲染假行、接口返回后覆盖——正是用户观察到的「默认无意义行被结果覆盖」；另发现前端二次 includes 过滤（破坏后端分页计数、null 字段 TypeError 风险）与 console.log 残留。
- 修复：mock 整块删除、初始空数组+v-loading、过滤分页全信后端、调试日志清除。
- 五页面默认行为系统检查（用户指令）全过：报告页（首载真实数据/查看弹窗不合格徽章+全文+时间线/重置）、记录页（筛选重置恢复全量）、计划页（seed 渲染/操作显隐）、人工核查（SO2/跨度口默认值+重置保持）、钢瓶气（实时浓度/降级正常）。


## 附 4：报告「设备恢复」无信息 + 两个浓度值差异（用户 review）✅

- **G-BUG-19 根因**：composer `goToRecoveryPhase()`（记录恢复阶段起止时间戳）只在 `stop()` 中断路径被调用，**正常完成路径漏激活**——恢复实际执行（日志实证 6/6 步 +70s，设备复原）但阶段时间线无 start/end，报告里「[跨度] 设备恢复」显示为空。修复=execute() 恢复链前补激活（防重入）。
- **短时 E2E 实证**（采纳用户提效建议：stableTime=60/采样 1 次/间隔 5s/恢复延迟 5s，全程 2 分钟 vs 默认 29 分钟）：五阶段时间线完整——安全初始化 37s/建立气路 9s/跨度检查 60s（=stableTime 精确）/自动校准「按需跳过」如实展示/**设备恢复 15s 入时间线与报告预览**。
- **两个浓度值的差异解释**（SO₂浓度 40.590 vs 响应/设备值 15.5）：15.5=检查阶段 `collectSamples` 多次采样的**均值**（判定依据，vs 标气 400ppb 得漂移 -96.1%）；40.590=流程收尾时刻的**实时瞬时快照**（`buildKeyParametersSnapshotAtComplete`，恰逢检查结束恢复开始的过渡态）。时刻与口径均不同，非数据错误——sim 环境读数随气体切换大幅漂移，真实设备恢复完成后回环境本底。
- **环境插曲（非缺陷）**：21:28 重启后 1-10 分钟内三次触发均「Failed to acquire lock」——重启后全量设备轮询风暴占用命令锁（已知架构缺陷：acquire 阻塞全局 2 线程池，memory 有档）；风暴窗口过后第四次执行 SUCCESS。建议：core 重启后等轮询稳定（约 10 分钟）再触发质控。


## 附 5：qcm 成功路径 E2E skill（用户指令：参考 composer-test skill 制作）✅

- **产出**：`.claude/skills/core-integration-test/skills/integration-qcm-plan-test/`（SKILL.md + scripts/test_qcm_plan_e2e.py，13 断言自动判定，非零退出码=失败）。
- **首跑全绿（13/13 PASS）**：创建短时计划（stableTime=8s/采样1次/间隔5s/恢复5s）→ 立即执行受理 → 终态 SUCCESS → **判定「校准任务完成，且已通过」**（sim 预设 so2=400ppb=标气浓度 → drift≈0，fixed 模式防波动）→ 触发溯源正确 → 5 阶段时间线齐（recovery 15s 起止完整，G-BUG-19 回归锁）→ job15 报告生成 → **span_calibration_result=合格** + 标气/漂移数据 → 清理。全程 ≈45 秒。
- **浏览器复核**：记录页首行「成功 | 质控结果-合格」（与上一行 22:08 的「不合格」对照——sim 值预设前后差异一目了然）。
- **关键环境发现（已入 SKILL 附录）**：① sim 改值后须经设备周期轮询才能到 ecat 属性缓存（flow 读缓存非直读），轮询周期内执行会读到旧值；② 21:28 重启风暴后 saimosen 周期轮询**停摆**（45 分钟无成功/失败日志，属性停更），重启 core 后恢复——E2E 前置须确认属性已传播（SKILL 已写明「so2 属性=400 复核」步骤）。


## 附 6：报告 SO₂浓度 1047.472 ppb（用户 review，2026-08-22）✅

- **根因（G-BUG-20）**：数值=µg/m³、标签=ppb 的单位错位。airdevice 逻辑属性 native 单位 µg/m³（ADM 国标口径定案），快照取值用裸 getDisplayValue() 拿到 native 值（400ppb×64.07/24.45=1047.47），标签逻辑无条件补 " ppb"。判定链路 getDisplayValue(PPB) 一直正确——判定/合格结论从未受影响，仅报告快照列错。
- **修复**：主浓度行带目标单位取值（SO2/NO2/O3→PPB、CO→PPM），与判定同口径、与 D19 单位规范一致；非浓度工况行不变。TDD 3 用例（ppb 口径/CO ppm/工况行不受影响）+ E2E 脚本新增快照断言（≈标气浓度±10% 且含 ppb，防复发）。
- **E2E 14/14 PASS**：快照 SO₂浓度=**400.000 ppb（判定同口径）**、判定通过、报告合格、恢复时间线完整。qcm 239/239 绿。
- **环境事件（与代码无关）**：部署重启撞上云库 PG recovery（08:00-09:13 约 1.2h 不可连接，Druid 初始化失败致 8080 503）——库恢复后重启 core 一切正常。已入 SKILL 排查表候选。
